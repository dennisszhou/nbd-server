[package]
name = "nbd-server"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
async-trait = "0.1"
nbd-control-plane = { path = "../nbd-control-plane" }

[dev-dependencies]
tokio = { version = "1.37", features = ["macros", "rt-multi-thread"] }
